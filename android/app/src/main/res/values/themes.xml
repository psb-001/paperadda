<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. The Compose UI is themed in Kotlin
         (see com.example.ui.theme.MyApplicationTheme); this style only backs
         the Activity window (splash background, edge-to-edge). -->
    <style name="Theme.MyApplication" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launcher_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash screen (Android 12+ SplashScreen API + backport).
         Shows the PaperAdda mark on white, then swaps to Theme.MyApplication. -->
    <style name="Theme.PaperAdda.Splash" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/paperadda_splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.MyApplication</item>
    </style>
</resources>
